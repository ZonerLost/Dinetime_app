class LoginModel {
  final String logo;          // app logo at top
  final String facebookIcon;  // small FB icon for the CTA
  final String instagramIcon; // small IG icon for the CTA (white)

  const LoginModel({
    required this.logo,
    required this.facebookIcon,
    required this.instagramIcon,
  });
}
